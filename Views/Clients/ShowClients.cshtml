@using NutriFitWeb.Services
@using Microsoft.AspNetCore.Identity
@model IEnumerable<NutriFitWeb.Models.Client>
@inject SignInManager<UserAccountModel> SignInManager
@inject UserManager<UserAccountModel> UserManager

@{
    ViewData["Title"] = "ShowClients";
}

<h1>ShowClients</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserAccountModel.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Height)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserAccountModel.UserName)  
            </td>
            <td>
                @if (item.Weight == null || item.Weight == 0)
                {
                    <p>N/A</p>
                }
                else
                {
                    @Html.DisplayFor(modelItem => item.Weight)
                }   
                    
            </td>
            <td>
                @if (item.Height == null || item.Height == 0)
                {
                    <p>N/A</p>
                }
                else
                {
                    @Html.DisplayFor(modelItem => item.Height)
                }
            </td>
            <td>
                @if(UserManager.GetUserId(User) == item.Gym.UserAccount.Id){
                   <a href="Clients/RemoveClientFromGym">
                   <input type="button" value="Remover Cliente" />
                   </a>

                }else if(item.Gym.UserAccount.Id == null){
                    <p>Add</p>
                }
                @*<a asp-action="Edit" asp-route-id="@item.ClientId">Edit</a> |*@
                <a asp-action="ClientDetails" asp-route-id="@item.ClientId">Details</a> 
                @*<a asp-action="Delete" asp-route-id="@item.ClientId">Delete</a>*@
            </td>

        </tr>
}
    </tbody>
</table>
