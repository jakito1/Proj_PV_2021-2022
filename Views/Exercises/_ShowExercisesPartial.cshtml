@model IEnumerable<NutriFitWeb.Models.Exercise>

@***REMOVED***
    ViewData["Title"] = "Exercícios";
***REMOVED***

<h2>@ViewData["Title"]</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                Nome do Exercício
            </th>
            <th>
                Descrição
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        ***REMOVED***
            @foreach (var item in Model.Select((value, i) => new ***REMOVED*** i, value ***REMOVED***)) ***REMOVED***
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.value.ExerciseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.value.ExerciseDescription)
            </td>
            <td>
                <a asp-action="DeleteExercise" asp-controller="Exercises" data-ajax="true" data-ajax-update="#_ShowExercisesPartialList"
                asp-route-id="@item.i"><img src=~/icons/removeIcon.svg/></a>
            </td>
        </tr>
    ***REMOVED***
        
***REMOVED***
    </tbody>
</table>
