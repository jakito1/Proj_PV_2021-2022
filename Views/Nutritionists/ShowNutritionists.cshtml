@model IEnumerable<NutriFitWeb.Models.Nutritionist>
@using NutriFitWeb.Services
@using NutriFitWeb.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<UserAccountModel> SignInManager
@inject UserManager<UserAccountModel> UserManager

@***REMOVED***
    ViewData["Title"] = "ShowNutritionists";
***REMOVED***

@***REMOVED***string? routeUrl = Url.RouteUrl(ViewContext.RouteData.Values);***REMOVED***
@***REMOVED***string? qsPath = ViewContext.HttpContext.Request.QueryString.Value;***REMOVED***
@***REMOVED***string? returnUrl = $"***REMOVED***routeUrl***REMOVED******REMOVED***qsPath***REMOVED***";***REMOVED***

<h1>Nutricionistas</h1>

<form asp-action="ShowNutritionists" asp-route-id=email method="get">
    <input type="text" name="email" placeholder="Insira o email do utilizador"/>
    <button type="submit"><i class="fa fa-search"></i></button>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NutritionistFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NutritionistLastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) ***REMOVED***
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NutritionistFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NutritionistLastName)
            </td>
            <td>
                @if(item.Gym == null)***REMOVED***
                     <a asp-action="AddNutritionistToGym" asp-route-id="@item.NutritionistId" asp-route-url=@returnUrl>Adicionar ao Ginásio</a>
            ***REMOVED***else if(UserManager.GetUserId(User) == item.Gym.UserAccountModel.Id)***REMOVED***                   
                     <a asp-action="RemoveNutritionistFromGym" asp-route-id="@item.NutritionistId" asp-route-url=@returnUrl>Remover do Ginásio</a> 
            ***REMOVED***

                <a asp-action="Edit" asp-route-id="@item.NutritionistId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NutritionistId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NutritionistId">Delete</a>
            </td>
        </tr>
***REMOVED***
    </tbody>
</table>

@if (!string.IsNullOrEmpty(qsPath))
***REMOVED***
    <a asp-action="ShowNutritionists">Back to List</a>
***REMOVED***
