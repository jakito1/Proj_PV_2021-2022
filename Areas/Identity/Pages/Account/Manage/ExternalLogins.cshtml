@page
@model ExternalLoginsModel
@***REMOVED***
    ViewData["Title"] = "Manage your external logins";
    ViewData["ActivePage"] = ManageNavPages.ExternalLogins;
***REMOVED***

<partial name="_StatusMessage" for="StatusMessage" />
@if (Model.CurrentLogins?.Count > 0)
***REMOVED***
    <h3>Registered Logins</h3>
    <table class="table">
        <tbody>
            @foreach (var login in Model.CurrentLogins)
            ***REMOVED***
                <tr>
                    <td id="@($"login-provider-***REMOVED***login.LoginProvider***REMOVED***")">@login.ProviderDisplayName</td>
                    <td>
                        @if (Model.ShowRemoveButton)
                        ***REMOVED***
                            <form id="@($"remove-login-***REMOVED***login.LoginProvider***REMOVED***")" asp-page-handler="RemoveLogin" method="post">
                                <div>
                                    <input asp-for="@login.LoginProvider" name="LoginProvider" type="hidden" />
                                    <input asp-for="@login.ProviderKey" name="ProviderKey" type="hidden" />
                                    <button type="submit" class="btn btn-primary" title="Remove this @login.ProviderDisplayName login from your account">Remove</button>
                                </div>
                            </form>
                    ***REMOVED***
                        else
                        ***REMOVED***
                            @: &nbsp;
                    ***REMOVED***
                    </td>
                </tr>
        ***REMOVED***
        </tbody>
    </table>
***REMOVED***
@if (Model.OtherLogins?.Count > 0)
***REMOVED***
    <h4>Add another service to log in.</h4>
    <hr />
    <form id="link-login-form" asp-page-handler="LinkLogin" method="post" class="form-horizontal">
        <div id="socialLoginList">
            <p>
                @foreach (var provider in Model.OtherLogins)
                ***REMOVED***
                    <button id="@($"link-login-button-***REMOVED***provider.Name***REMOVED***")" type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
            ***REMOVED***
            </p>
        </div>
    </form>
***REMOVED***
