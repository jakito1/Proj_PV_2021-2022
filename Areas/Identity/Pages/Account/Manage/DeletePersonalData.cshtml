@page
@model DeletePersonalDataModel
@***REMOVED***
    ViewData["Title"] = "Delete Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
***REMOVED***

<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Apagar estes dados irá remover a sua conta permanentemente, não podendo ser recuperada.</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @if (Model.RequirePassword)
        ***REMOVED***
            <div class="form-floating">
                <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" />
                <label asp-for="Input.Password" class="form-label"></label>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
    ***REMOVED***
        <button class="w-100 btn btn-lg btn-danger" type="submit">Apagar dados e remover a minha conta</button>
    </form>
</div>

@section Scripts ***REMOVED***
    <partial name="_ValidationScriptsPartial" />
***REMOVED***
